<?xml version="1.0" encoding="UTF-8"?>
<!-- 日志级别从低到高分为TRACE < DEBUG < INFO < WARN < ERROR < FATAL，如果设置为WARN，则低于WARN的信息都不会输出 -->
<!-- scan:当此属性设置为true时，配置文件如果发生改变，将会被重新加载，默认值为true -->
<!-- scanPeriod:设置监测配置文件是否有修改的时间间隔，如果没有给出时间单位，默认单位是毫秒。当scan为true时，此属性生效。默认的时间间隔为1分钟。 -->
<!-- debug:当此属性设置为true时，将打印出logback内部日志信息，实时查看logback运行状态。默认值为false。 -->
<configuration scan="true" scanPeriod="10 seconds">

    <!--include resource="logback-spring-${spring.profiles.active}.xml"/-->
    <springProperty scope="context" name="prod-name" source="spring.application.name" defaultValue="assistant"/>
    <springProperty scope="context" name="log-path" source="loging.file.path" defaultValue="/opt/logs"/>
    <springProperty scope="context" name="max-history" source="loging.file.max-history" defaultValue="365"/>

    <contextName>logback</contextName>
    <!-- 指定脱敏类的位置 -->
<!--    <conversionRule conversionWord="msg" class="com.vivi.plugin.logs.SensitiveLogDataConverter"/>-->

    <!-- 普通日志格式 -->
    <property name="CONSOLE_LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%-13.13thread{13}] %-5level %-40.40logger{39} : %msg%n"/>
    <property name="FILE_LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%-13.13thread{13}] %-5level %-40.40logger{39} : %msg%n"/>

    <!--输出到控制台-->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <!--此日志appender是为开发使用，只配置最底级别，控制台输出的日志级别是大于或等于此级别的日志信息-->
        <!--filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>info</level>
        </filter-->
        <encoder>
            <Pattern>${CONSOLE_LOG_PATTERN}</Pattern>
            <!-- 设置字符集 -->
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!--输出到文件-->
    <!-- 时间滚动输出日志 -->
    <appender name="LOG_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!-- 支持多个 JVM 同时写一个文件,也叫严格模式，该模式为开启时，滚动策略只能使用TimeBasedRollingPolicy及其子策略 -->
        <!-- 同时，该模式开启时不支持设置file标签及文件压缩,所以这一点非常终要!!!!! -->
        <!-- prudent>false</prudent-->
        <!-- 正在记录的日志文件的路径及文件名 -->
        <file>${log-path}/log-${prod-name}.log</file>
        <!--日志文件输出格式-->
        <encoder>
            <Pattern>${FILE_LOG_PATTERN}</Pattern>
            <charset>UTF-8</charset>
        </encoder>
        <!-- 日志记录器的滚动策略，按日期，按大小记录 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- 每天日志归档路径以及格式 -->
            <fileNamePattern>${log-path}/his-${prod-name}/log-${prod-name}-%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <!--日志文件保留天数-->
            <maxHistory>${max-history}</maxHistory>
        </rollingPolicy>
    </appender>

    <!-- 异步输出 日志 -->
    <appender name="ASYNC" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="LOG_FILE"/>
        <!-- 默认队列深度:默认256 / 设置异步阻塞队列的大小，为了不丢失日志建议设置的大一些，单机压测时100000是没问题的，应该不用担心OOM -->
        <!--queueSize>256</queueSize-->
        <!-- 设置丢弃DEBUG、TRACE、INFO日志的阀值，不丢失 -->
        <!-- 如果队列80%已满，则丢弃 tract、debug、info级别日志 / 设置丢弃DEBUG、TRACE、INFO日志的阀值，不丢失 -->
        <!-- discardingThreshold>0</discardingThreshold -->
        <!-- 设置队列入队时非阻塞，当队列满时会直接丢弃日志，但是对性能提升极大 -->
        <neverBlock>true</neverBlock>
    </appender>

    <springProfile name="dev">
        <root level="info">
            <appender-ref ref="CONSOLE" />
            <appender-ref ref="ASYNC" />
        </root>
        <!-- mybatis sql单独控制输出级别 -->
        <logger name="com.**.mapper" level="debug"/>
    </springProfile>

    <root level="info">
        <appender-ref ref="ASYNC"/>
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>
